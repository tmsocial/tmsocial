import { RelativeNowPipe } from './relative-now.pipe';

describe('RelativeNowPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeNowPipe();
    expect(pipe).toBeTruthy();
  });
});
